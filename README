##Crear una nueva rama##
 git branch nombre rama
##Establecerse en la nueva rama##
 git checkout nombre rama
##Subir la rama##
 git push -u origin nombre rama
##Si  error en tu correo-solucion##
configura el correo 
git config --global user.email "ejemplo@gmail.com"
##Tu usuario##
 git config --global user.name "usuario"

 
# Ver el estado de los archivos
git status

# Agregar los cambios al área de preparación
git add nombre_del_archivo.ext

# Confirmar los cambios
git commit -m "Descripción de los cambios realizados"

# Subir los cambios al repositorio remoto (si tienes uno configurado)
git push origin main
